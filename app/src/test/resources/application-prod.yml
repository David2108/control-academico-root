spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/control_academico
    username: dev
    password: dev
  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    contexts: prod
    parameters:
      datetime_type: TIMESTAMP WITHOUT TIME ZONE